Public Class Controlador_Cliente


    Public Function Buscar_Cliente(ByVal Tipo_Identidad As String, ByVal Numero As String, ByVal Venta As Realizar_Venta) As Boolean
        Dim Cliente As Cliente
        Dim a As Boolean
        Cliente = New Cliente
        'a = Cliente.Buscar_CI(Tipo_Identidad, Numero)
    End Function
End Class
